<nav>
    <ul class="usa-sidenav-list">
        {{#foreach navigation}}
        <li class="{{#if current}}usa-current{{/if}}" role="presentation">
            <a href="{{url absolute="true"}}">{{label}}</a>
        </li>
        {{/foreach}}
    </ul>
</nav>
